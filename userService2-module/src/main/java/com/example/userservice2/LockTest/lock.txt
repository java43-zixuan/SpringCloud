锁：
重入锁、读写锁、排他锁、乐观锁、悲观锁、自旋锁、轻量级锁、重量级锁、公平锁、非公平锁（Synchronized默认是非公平锁）......

重入锁

读写锁:
读和读不互斥
读和写、写和写互斥
锁的设计？
1、需求分析
同一时刻只允许一个线程获得锁

是否可重入（要记住当前获得锁的线程，判断再次获得锁的线程是否与当前线程一致）
锁的排他性（一个全局变量作为锁的状态）
没有获得锁的线程进行等待（队列来存储等待获取锁的线程）
等待的线程需要被唤醒（LockSupport.unpark(thread)唤醒指定线程）
竞争锁的线程是否可以被中断
锁的公平性


